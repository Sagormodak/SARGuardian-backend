# Database migrations

This directory is reserved for Alembic migrations. The initial application schema is defined in `app.models`; run `python scripts/init_db.py` for local development until the first migration revision is added.
